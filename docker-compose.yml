services:
  netcode-server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    container_name: netcode-engine-server
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
    restart: unless-stopped
